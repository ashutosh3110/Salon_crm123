// AppHeader is now embedded directly in each page for the reference UI.
// This component is kept as a no-op to avoid import errors if referenced.
export default function AppHeader() {
    return null;
}
