# Copy to .env.local. Backend is only at /v1 (NOT /api/v1).
# Use either:
#   VITE_API_URL=http://localhost:3000
#   VITE_API_URL=http://localhost:3000/v1
# Avoid: .../api/v1 (wrong path → 404 on petty cash & other APIs)
VITE_API_URL=http://localhost:3000
