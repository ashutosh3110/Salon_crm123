import receptionistData from './receptionistMockData.json';

export const {
    appointments,
    pendingCheckins,
    checkoutRequested,
    invoices,
    recentPayments,
    waitingList,
    stylists,
    dashboardStats,
    invoiceStats,
    settingsSections
} = receptionistData;
